-- ==================== 基础工具函数 ====================

-- 屏幕尺寸访问器
local Screen = setmetatable({}, {
    __index = function(self, key)
        local cam = workspace.CurrentCamera
        local size = cam and cam.ViewportSize or Vector2.new(1920, 1080)
        if key == "Width" then return size.X
        elseif key == "Height" then return size.Y
        elseif key == "Size" then return size end
    end
})

local UserInputService = game:GetService("UserInputService")
local ViewportSize = workspace.CurrentCamera.ViewportSize

-- 根据屏幕尺寸缩放数值（适配移动端/桌面）
local function scale(axis, value)
    local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
    local baseWidth, baseHeight = 1920, 1080
    local scaleFactor = isMobile and 2 or 1.5
    if axis == "X" then
        return value * (ViewportSize.X / baseWidth) * scaleFactor
    elseif axis == "Y" then
        return value * (ViewportSize.Y / baseHeight) * scaleFactor
    end
end

-- 安全调用函数（兼容不同执行环境）
local function safeCall(typeCheck, func, fallback)
    if type(func) == typeCheck then return func end
    return fallback
end

-- 封装 cloneref（跨环境兼容）
local cloneref = safeCall("function", cloneref, function(...) return ... end)

-- 服务容器
local Services = setmetatable({}, {
    __index = function(self, serviceName)
        return cloneref(game:GetService(serviceName))
    end
})

-- 常用服务
local Players = Services.Players
local RunService = Services.RunService
local UserInputService = Services.UserInputService
local TweenService = Services.TweenService
local AvatarEditorService = Services.AvatarEditorService
local HttpService = Services.HttpService

-- 玩家与角色
local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local isR6 = humanoid.RigType == Enum.HumanoidRigType.R6
local lastPosition = character.PrimaryPart and character.PrimaryPart.Position or Vector3.new()

-- 角色重生事件
player.CharacterAdded:Connect(function(newChar)
    character = newChar
    humanoid = newChar:WaitForChild("Humanoid")
    isR6 = humanoid.RigType == Enum.HumanoidRigType.R6
    -- 更新 UI 标题和标签（稍后定义）
    if titleLabel then
        titleLabel.Text = isR6 and "GAZE SMOTES" or "Gaze Emotes"
    end
    if catalogTabBtn then
        catalogTabBtn.Text = isR6 and "Anim" or "Catalog"
    end
    if savedTabBtn then
        savedTabBtn.Visible = not isR6
    end
    if isR6 and savedTabBtn and catalogTabBtn then
        savedTabBtn.Visible = true
        catalogTabBtn.Visible = false
        catalogTabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 80)
        savedTabBtn.BackgroundColor3 = Color3.fromRGB(20, 50, 20)
    end
    lastPosition = newChar.PrimaryPart and newChar.PrimaryPart.Position or Vector3.new()
end)

-- ==================== 设置表 ====================

local Settings = {}
Settings["Stop Emote When Moving"] = true
Settings["Fade In"] = 0.1
Settings["Fade Out"] = 0.1
Settings["Weight"] = 1
Settings["Speed"] = 1
Settings["Time Position"] = 0
Settings["Freeze On Finish"] = false
Settings["Looped"] = true
Settings["Stop Other Animations On Play"] = true
Settings["High Priority"] = true

-- ==================== 保存的表情列表 ====================

local savedEmotes = {}
local SAVE_FILE = "GazeEmotes_NewNEWN3WSaved.json"

-- 加载保存的表情
local function loadSavedEmotes()
    local success, data = pcall(function()
        if readfile and isfile and isfile(SAVE_FILE) then
            return HttpService:JSONDecode(readfile(SAVE_FILE))
        end
        return {}
    end)
    if success and type(data) == "table" then
        savedEmotes = data
    else
        savedEmotes = {}
    end
    for _, emote in ipairs(savedEmotes) do
        if not emote.AnimationId then
            if emote.AssetId then
                emote.AnimationId = "rbxassetid://" .. tostring(emote.AssetId)
            else
                emote.AnimationId = "rbxassetid://" .. tostring(emote.Id)
            end
        end
        if emote.Favorite == nil then
            emote.Favorite = false
        end
    end
end

-- 保存表情到文件
local function saveEmotesToFile()
    pcall(function()
        if writefile then
            writefile(SAVE_FILE, HttpService:JSONEncode(savedEmotes))
        end
    end)
end

loadSavedEmotes()

-- ==================== 动画播放系统 ====================

local currentTrack = nil

-- 加载并播放指定 ID 的动画
local function loadAndPlayTrack(assetId)
    if currentTrack then
        currentTrack:Stop(Settings["Fade Out"])
    end

    local animId
    local success, objects = pcall(function()
        return game:GetObjects("rbxassetid://" .. tostring(assetId))
    end)
    if success and objects and #objects > 0 then
        local obj = objects[1]
        if obj:IsA("Animation") then
            animId = obj.AnimationId
        else
            animId = "rbxassetid://" .. tostring(assetId)
        end
    else
        animId = "rbxassetid://" .. tostring(assetId)
    end

    local newAnim = Instance.new("Animation")
    newAnim.AnimationId = animId

    local newTrack = humanoid:LoadAnimation(newAnim)
    local priority = Settings["High Priority"] and Enum.AnimationPriority.Action4 or Enum.AnimationPriority.Action
    newTrack.Priority = priority

    local weight = Settings["Weight"]
    if weight == 0 then weight = 0.001 end

    if Settings["Stop Other Animations On Play"] then
        for _, track in pairs(humanoid.Animator:GetPlayingAnimationTracks()) do
            if track.Priority ~= priority then
                track:Stop()
            end
        end
    end

    newTrack:Play(Settings["Fade In"], weight, Settings["Speed"])
    currentTrack = newTrack
    currentTrack.TimePosition = math.clamp(Settings["Time Position"], 0, 1) * (currentTrack.Length or 1)
    currentTrack.Priority = priority
    currentTrack.Looped = Settings["Looped"]

    return newTrack
end

-- 每帧检测移动停止动画
RunService.RenderStepped:Connect(function()
    if Settings["Looped"] and currentTrack and currentTrack.IsPlaying then
        currentTrack.Looped = Settings["Looped"]
    end

    if character:FindFirstChild("HumanoidRootPart") then
        local root = character.HumanoidRootPart
        if Settings["Stop Emote When Moving"] and currentTrack and currentTrack.IsPlaying then
            local moved = (root.Position - lastPosition).Magnitude > 0.1
            local jumping = humanoid and humanoid:GetState() == Enum.HumanoidStateType.Jumping
            if moved or jumping then
                currentTrack:Stop(Settings["Fade Out"])
                currentTrack = nil
            end
        end
        lastPosition = root.Position
    end
end)
-- ==================== GUI 构建 ====================

local CoreGui = game:GetService("CoreGui")

-- 主 GUI
local gui = Instance.new("ScreenGui")
gui.Name = "GazeEmoteGUI"
gui.Parent = CoreGui
gui.Enabled = false  -- 初始隐藏，由切换按钮控制
gui.DisplayOrder = 999

-- 创建圆角
local function createCorner(instance, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius or 6)
    corner.Parent = instance
    return corner
end

-- 创建边框描边
local function createStroke(instance, color, thickness)
    local stroke = Instance.new("UIStroke")
    stroke.Color = color or Color3.fromRGB(60, 120, 200)
    stroke.Thickness = thickness or 1
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    stroke.Parent = instance
    return stroke
end

-- 缩放动画（卡片入场）
local function scaleIn(instance)
    local uiScale = Instance.new("UIScale")
    uiScale.Scale = 0.5
    uiScale.Parent = instance
    TweenService:Create(uiScale, TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), { Scale = 1 }):Play()
end

-- 主容器
local mainContainer = Instance.new("Frame")
mainContainer.Size = UDim2.new(0, scale("X", 870), 0, scale("Y", 750))  -- 原混淆脚本中为 470x450，但实际计算值较大
mainContainer.Position = UDim2.new(0.5, -scale("X", 485), 0.5, -scale("Y", 405))
mainContainer.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
mainContainer.BackgroundTransparency = 0.15
mainContainer.Active = true
mainContainer.Draggable = true
mainContainer.Parent = gui
createCorner(mainContainer, 8)
createStroke(mainContainer, Color3.fromRGB(80, 80, 120), 1.5)

-- 右下角缩放手柄
local resizeHandle = Instance.new("TextButton")
resizeHandle.Size = UDim2.new(0, 24, 0, 24)
resizeHandle.Position = UDim2.new(1, -24, 1, -24)
resizeHandle.BackgroundTransparency = 1
resizeHandle.Text = "◢"
resizeHandle.TextColor3 = Color3.fromRGB(100, 100, 140)
resizeHandle.TextSize = 18
resizeHandle.ZIndex = 10
resizeHandle.Parent = mainContainer

-- 拖拽缩放逻辑
local isResizing = false
local resizeStartPos, resizeStartSize
resizeHandle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        isResizing = true
        resizeStartPos = input.Position
        resizeStartSize = mainContainer.AbsoluteSize
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if isResizing and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - resizeStartPos
        local newWidth = math.max(150, resizeStartSize.X + delta.X)
        local newHeight = math.max(100, resizeStartSize.Y + delta.Y)
        mainContainer.Size = UDim2.new(0, newWidth, 0, newHeight)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        isResizing = false
    end
end)

-- 标题栏
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, scale("Y", 36))
titleLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
titleLabel.BackgroundTransparency = 0.5
titleLabel.Text = isR6 and "Gaze Emotes (R6)" or "Gaze Emotes"
titleLabel.TextColor3 = Color3.new(1, 1, 1)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextScaled = true
titleLabel.Parent = mainContainer
createCorner(titleLabel, 8)

-- 标签按钮：Catalog / Saved
local catalogTabBtn = Instance.new("TextButton")
catalogTabBtn.Size = UDim2.new(0.305, 0, 0, scale("Y", 24))
catalogTabBtn.Position = UDim2.new(0.052, 0, 0, scale("Y", 40))
catalogTabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 80)
catalogTabBtn.BackgroundTransparency = 0.205
catalogTabBtn.Text = isR6 and "Animation" or "Catalog"
catalogTabBtn.TextColor3 = Color3.new(1, 1, 1)
catalogTabBtn.Font = Enum.Font.GothamBold
catalogTabBtn.TextScaled = true
catalogTabBtn.Parent = mainContainer
createCorner(catalogTabBtn, 4)

local savedTabBtn = Instance.new("TextButton")
savedTabBtn.Size = UDim2.new(0.298, 0, 0, scale("Y", 24))
savedTabBtn.Position = UDim2.new(0.355, 0, 0, scale("Y", 40))
savedTabBtn.BackgroundColor3 = Color3.fromRGB(30, 80, 30)
savedTabBtn.BackgroundTransparency = 0.204
savedTabBtn.Text = "Saved"
savedTabBtn.TextColor3 = Color3.new(1, 1, 1)
savedTabBtn.Font = Enum.Font.GothamBold
savedTabBtn.TextScaled = true
savedTabBtn.Visible = not isR6
savedTabBtn.Parent = mainContainer
createCorner(savedTabBtn, 4)

-- 分割线
local divider = Instance.new("Frame")
divider.Size = UDim2.new(0, scale("X", 2), 1, -scale("Y", 70))
divider.Position = UDim2.new(0.605, -scale("X", 1), 0, scale("Y", 125))
divider.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
divider.BackgroundTransparency = 0.502
divider.Parent = mainContainer

-- 左侧内容区域（Catalog 和 Saved 共用）
local leftContent = Instance.new("Frame")
leftContent.Size = UDim2.new(0.607, -scale("X", 193), 1, -scale("Y", 306))
leftContent.Position = UDim2.new(0, scale("X", 196), 0, scale("Y", 308))
leftContent.BackgroundTransparency = 249
leftContent.Visible = true
leftContent.Parent = mainContainer

-- ========== Catalog 页面 ==========
local catalogFrame = Instance.new("Frame")
catalogFrame.Size = UDim2.new(1, 256, 266, 273)
catalogFrame.BackgroundTransparency = 277
catalogFrame.Visible = true
catalogFrame.Parent = leftContent

-- 搜索框
local searchBox = Instance.new("TextBox")
searchBox.Size = UDim2.new(0.567, -scale("X", 181), 262, scale("Y", 251))
searchBox.Position = UDim2.new(0, scale("X", 296), 312, 319)
searchBox.PlaceholderText = "Search..."
searchBox.BackgroundColor3 = Color3.fromRGB(326, 329, 335)
searchBox.BackgroundTransparency = 338
searchBox.TextColor3 = Color3.new(1, 343, 346)
searchBox.Font = Enum.Font.Gotham
searchBox.TextScaled = true
searchBox.ClearTextOnFocus = false
searchBox.Text = ""
searchBox.Parent = catalogFrame
createCorner(searchBox, 359)
createStroke(searchBox, Color3.fromRGB(362, 366, 370), 376)

-- 刷新按钮
local refreshBtn = Instance.new("TextButton")
refreshBtn.Size = UDim2.new(0.189, -scale("X", 351), 356, scale("Y", 360))
refreshBtn.Position = UDim2.new(0.574, scale("X", 367), 372, 378)
refreshBtn.BackgroundColor3 = Color3.fromRGB(383, 390, 398)
refreshBtn.BackgroundTransparency = 404
refreshBtn.Text = "Refresh"
refreshBtn.Font = Enum.Font.GothamBold
refreshBtn.TextScaled = true
refreshBtn.TextColor3 = Color3.new(1, 412, 416)
refreshBtn.Parent = catalogFrame
createCorner(refreshBtn, 421)

-- 排序按钮
local sortBtn = Instance.new("TextButton")
sortBtn.Size = UDim2.new(0.188, -scale("X", 426), 431, scale("Y", 436))
sortBtn.Position = UDim2.new(0.765, scale("X", 422), 430, 436)
sortBtn.BackgroundColor3 = Color3.fromRGB(458, 461, 466)
sortBtn.BackgroundTransparency = 471
sortBtn.Text = "Sort: Relevance"
sortBtn.Font = Enum.Font.GothamBold
sortBtn.TextScaled = true
sortBtn.TextColor3 = Color3.new(1, 479, 482)
sortBtn.Parent = catalogFrame
createCorner(sortBtn, 489)

-- 滚动区域（表情卡片）
local emotesScroll = Instance.new("ScrollingFrame")
emotesScroll.Size = UDim2.new(1, -scale("X", 494), 499, -scale("Y", 503))
emotesScroll.Position = UDim2.new(0, scale("X", 508), 513, scale("Y", 518))
emotesScroll.CanvasSize = UDim2.new(0, 523, 528, 533)
emotesScroll.ScrollBarThickness = 537
emotesScroll.ScrollBarImageColor3 = Color3.fromRGB(540, 544, 548)
emotesScroll.BackgroundTransparency = 552
emotesScroll.Parent = catalogFrame

-- 网格布局
local emotesLayout = Instance.new("UIGridLayout", emotesScroll)
emotesLayout.CellSize = UDim2.new(0, scale("X", 563), 568, scale("Y", 573))
emotesLayout.CellPadding = UDim2.new(0, scale("X", 576), 581, scale("Y", 586))

-- 空状态提示
local emptyLabel = Instance.new("TextLabel", emotesScroll)
emptyLabel.Size = UDim2.new(1, 678, 682, scale("Y", 687))
emptyLabel.Position = UDim2.new(0, 692, 698, -scale("Y", 702))
emptyLabel.BackgroundTransparency = 706
emptyLabel.Text = "Nothing Silly Here :3 (except me)"
emptyLabel.TextColor3 = Color3.new(1, 711, 715)
emptyLabel.Font = Enum.Font.GothamBold
emptyLabel.TextScaled = true
emptyLabel.Visible = false

-- 分页按钮
local prevBtn = Instance.new("TextButton", catalogFrame)
prevBtn.Size = UDim2.new(0.407, -scale("X", 723), 730, scale("Y", 735))
prevBtn.Position = UDim2.new(0.009, scale("X", 740), 745, -scale("Y", 749))
prevBtn.BackgroundColor3 = Color3.fromRGB(753, 758, 762)
prevBtn.BackgroundTransparency = 767
prevBtn.Text = "< Prev"
prevBtn.Font = Enum.Font.GothamBold
prevBtn.TextScaled = true
prevBtn.TextColor3 = Color3.new(1, 773, 776)
createCorner(prevBtn, 781)

local nextBtn = Instance.new("TextButton", catalogFrame)
nextBtn.Size = UDim2.new(0.406, -scale("X", 786), 791, scale("Y", 794))
nextBtn.Position = UDim2.new(0.587, scale("X", 800), 806, -scale("Y", 812))
nextBtn.BackgroundColor3 = Color3.fromRGB(817, 822, 827)
nextBtn.BackgroundTransparency = 832
nextBtn.Text = "Next >"
nextBtn.Font = Enum.Font.GothamBold
nextBtn.TextScaled = true
nextBtn.TextColor3 = Color3.new(1, 839, 842)
createCorner(nextBtn, 847)

-- 页码输入框
local pageBox = Instance.new("TextBox", catalogFrame)
pageBox.Size = UDim2.new(0.199, 852, 857, scale("Y", 862))
pageBox.Position = UDim2.new(0.388, scale("X", 867), 872, -scale("Y", 877))
pageBox.BackgroundTransparency = 882
pageBox.Font = Enum.Font.Gotham
pageBox.TextScaled = true
pageBox.TextColor3 = Color3.new(1, 887, 890)
pageBox.Text = "1 / Enter page"

-- 页码错误提示
local pageNotif = Instance.new("TextLabel", catalogFrame)
pageNotif.Size = UDim2.new(0.312, 895, 900, scale("Y", 904))
pageNotif.Position = UDim2.new(0.331, 909, 914, -scale("Y", 921))
pageNotif.BackgroundTransparency = 925
pageNotif.TextColor3 = Color3.fromRGB(928, 933, 936)
pageNotif.Font = Enum.Font.Gotham
pageNotif.TextScaled = true
pageNotif.Text = ""
pageNotif.Visible = false

-- ========== Saved 页面 ==========
local savedFrame = Instance.new("Frame")
savedFrame.Size = UDim2.new(0.613, -scale("X", 942), 947, -scale("Y", 952))
savedFrame.Position = UDim2.new(0, scale("X", 957), 961, scale("Y", 966))
savedFrame.BackgroundTransparency = 971
savedFrame.Visible = false
savedFrame.Parent = leftContent

-- Saved 搜索框
local savedSearch = Instance.new("TextBox")
savedSearch.Size = UDim2.new(0.672, -scale("X", 976), 981, scale("Y", 987))
savedSearch.Position = UDim2.new(0, scale("X", 992), 996, 1002)
savedSearch.PlaceholderText = "Search Saved..."
savedSearch.BackgroundColor3 = Color3.fromRGB(1007, 1011, 1015)
savedSearch.BackgroundTransparency = 1019
savedSearch.TextColor3 = Color3.new(1, 1025, 1028)
savedSearch.Font = Enum.Font.Gotham
savedSearch.TextScaled = true
savedSearch.ClearTextOnFocus = false
savedSearch.Text = ""
savedSearch.Parent = savedFrame
createCorner(savedSearch, 1036)
createStroke(savedSearch, Color3.fromRGB(1039, 1043, 1047), 1052)

-- ID 输入框
local idInput = Instance.new("TextBox")
idInput.Size = UDim2.new(0.197, 1056, 1061, scale("Y", 1066))
idInput.Position = UDim2.new(0.678, -scale("X", 994), 998, 1004)
idInput.PlaceholderText = "Emote ID"
idInput.BackgroundColor3 = Color3.fromRGB(1009, 975, 979)
idInput.BackgroundTransparency = 984
idInput.TextColor3 = Color3.new(1, 990, 995)
idInput.Font = Enum.Font.Gotham
idInput.TextScaled = true
idInput.ClearTextOnFocus = false
idInput.Text = ""
idInput.Parent = savedFrame
createCorner(idInput, 1000)
createStroke(idInput, Color3.fromRGB(1006, 960, 965), 970)

-- 添加 ID 按钮
local addIdBtn = Instance.new("TextButton")
addIdBtn.Size = UDim2.new(0.095, 980, 986, scale("Y", 989))
addIdBtn.Position = UDim2.new(0.879, 955, 958, 963)
addIdBtn.BackgroundColor3 = Color3.fromRGB(969, 974, 988)
addIdBtn.BackgroundTransparency = 932
addIdBtn.Text = "+"
addIdBtn.Font = Enum.Font.GothamBold
addIdBtn.TextScaled = true
addIdBtn.TextColor3 = Color3.new(1, 938, 942)
addIdBtn.Parent = savedFrame
createCorner(addIdBtn, 946)

-- Saved 滚动区域
local savedScroll = Instance.new("ScrollingFrame")
savedScroll.Size = UDim2.new(1, -scale("X", 951), 956, -scale("Y", 896))
savedScroll.Position = UDim2.new(0, scale("X", 907), 912, scale("Y", 918))
savedScroll.CanvasSize = UDim2.new(0, 923, 927, 935)
savedScroll.ScrollBarThickness = 940
savedScroll.BackgroundTransparency = 945
savedScroll.Parent = savedFrame

-- Saved 空状态
local savedEmptyLabel = Instance.new("TextLabel")
savedEmptyLabel.Size = UDim2.new(1, 950, 855, scale("Y", 860))
savedEmptyLabel.Position = UDim2.new(0, 865, 871, -scale("Y", 880))
savedEmptyLabel.BackgroundTransparency = 889
savedEmptyLabel.Text = "Sorry I Was Changing Save Files Again 😅"
savedEmptyLabel.TextColor3 = Color3.new(1, 894, 897)
savedEmptyLabel.Font = Enum.Font.GothamBold
savedEmptyLabel.TextScaled = true
savedEmptyLabel.Visible = false
savedEmptyLabel.Parent = savedScroll

local savedLayout = Instance.new("UIGridLayout", savedScroll)
savedLayout.CellSize = UDim2.new(0, scale("X", 802), 807, scale("Y", 813))
savedLayout.CellPadding = UDim2.new(0, scale("X", 818), 823, scale("Y", 829))

-- ========== 设置面板（右侧） ==========
local settingsFrame = Instance.new("Frame")
settingsFrame.Size = UDim2.new(0.386, -scale("X", 835), 840, -scale("Y", 846))
settingsFrame.Position = UDim2.new(0.614, scale("X", 851), 856, scale("Y", 861))
settingsFrame.BackgroundTransparency = 866
settingsFrame.Parent = mainContainer

local settingsTitle = Instance.new("TextLabel")
settingsTitle.Size = UDim2.new(1, 868, 811, scale("Y", 816))
settingsTitle.BackgroundTransparency = 821
settingsTitle.Text = "Settings"
settingsTitle.TextColor3 = Color3.new(1, 787, 793)
settingsTitle.Font = Enum.Font.GothamBold
settingsTitle.TextScaled = true
settingsTitle.Parent = settingsFrame

-- 设置滚动区域
local settingsScroll = Instance.new("ScrollingFrame")
settingsScroll.Size = UDim2.new(1, -scale("X", 777), 783, -scale("Y", 788))
settingsScroll.Position = UDim2.new(0, scale("X", 744), 747, scale("Y", 734))
settingsScroll.BackgroundTransparency = 741
settingsScroll.CanvasSize = UDim2.new(0, 727, 732, 737)
settingsScroll.ScrollBarThickness = 720
settingsScroll.ScrollBarImageColor3 = Color3.fromRGB(717, 721, 724)
settingsScroll.Parent = settingsFrame

-- 锁定水平滚动
local function lockHorizontalScroll()
    settingsScroll.CanvasPosition = Vector2.new(0, settingsScroll.CanvasPosition.Y)
end
settingsScroll:GetPropertyChangedSignal("CanvasPosition"):Connect(lockHorizontalScroll)

-- 设置列表布局
local settingsLayout = Instance.new("UIListLayout", settingsScroll)
settingsLayout.Padding = UDim.new(0, 710)
settingsLayout.FillDirection = Enum.FillDirection.Vertical
settingsLayout.SortOrder = Enum.SortOrder.LayoutOrder

settingsLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    settingsScroll.CanvasSize = UDim2.new(0, 704, 708, settingsLayout.AbsoluteContentSize.Y + 705)
end)
-- ==================== 搜索逻辑 ====================

-- 排序模式
local sortModes = {
    {Enum.CatalogSortType.Relevance, "Relevance"},
    {Enum.CatalogSortType.PriceHighToLow, "Price High→Low"},
    {Enum.CatalogSortType.PriceLowToHigh, "Price Low→High"},
    {Enum.CatalogSortType.MostFavorited, "Most Favorited"},
    {Enum.CatalogSortType.RecentlyCreated, "Recently Created"},
    {Enum.CatalogSortType.Bestselling, "Bestselling"},
}
local currentSortIndex = 1
local currentKeyword = ""
local currentPages = nil
local currentPageNumber = 1

-- 获取搜索结果页
local function getPages(keyword)
    local params = CatalogSearchParams.new()
    params.SearchKeyword = keyword or ""
    params.CategoryFilter = Enum.CatalogCategoryFilter.None
    params.SalesTypeFilter = Enum.SalesTypeFilter.All
    params.AssetTypes = { Enum.AvatarAssetType.EmoteAnimation }
    params.IncludeOffSale = true
    params.SortType = sortModes[currentSortIndex][1]
    params.Limit = 10
    local ok, pages = pcall(function()
        return AvatarEditorService:SearchCatalog(params)
    end)
    if not ok then return nil end
    return pages
end

-- 获取真实动画ID（处理嵌套对象）
local function getRealAnimationId(assetId)
    local success, objects = pcall(function()
        return game:GetObjects("rbxassetid://" .. tostring(assetId))
    end)
    if not success or not objects or #objects == 0 then return nil end
    local obj = objects[1]
    if obj:IsA("Animation") and obj.AnimationId ~= "" then
        return tonumber(obj.AnimationId:match("%d+"))
    elseif obj:FindFirstChildOfClass("Animation") then
        local anim = obj:FindFirstChildOfClass("Animation")
        return tonumber(anim.AnimationId:match("%d+"))
    end
    return nil
end

-- ==================== 卡片创建 ====================

-- 创建表情卡片（Catalog 页面使用）
local function createEmoteCard(item)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(0, scale("X", 170), 0, scale("Y", 244))
    card.BackgroundColor3 = Color3.fromRGB(38, 48, 56)
    card.BackgroundTransparency = 0.082
    createCorner(card, 10)
    createStroke(card, Color3.fromRGB(52, 66, 76), 1.5)

    local assetId = item.AssetId or item.Id

    -- 缩略图
    local thumbnail = Instance.new("ImageLabel")
    thumbnail.Size = UDim2.new(1, -12, 0, 138)
    thumbnail.Position = UDim2.new(0, 6, 0, 6)
    thumbnail.BackgroundColor3 = Color3.fromRGB(31, 41, 54)
    thumbnail.BackgroundTransparency = 0.061
    thumbnail.ScaleType = Enum.ScaleType.Fit
    pcall(function()
        thumbnail.Image = "rbxthumb://type=Asset&id=" .. tonumber(assetId) .. "&w=150&h=150"
    end)
    thumbnail.Parent = card
    createCorner(thumbnail, 8)

    -- 名称
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1, -12, 0, 38)
    nameLabel.Position = UDim2.new(0, 6, 0, 152)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = item.Name or "Unknown"
    nameLabel.TextColor3 = Color3.new(1, 1, 1)
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.TextScaled = true
    nameLabel.TextWrapped = true
    nameLabel.Parent = card

    -- 复制链接按钮
    local copyBtn = Instance.new("TextButton")
    copyBtn.Size = UDim2.new(0, 32, 0, 32)
    copyBtn.Position = UDim2.new(1, -38, 0, 148)
    copyBtn.BackgroundColor3 = Color3.fromRGB(44, 53, 63)
    copyBtn.BackgroundTransparency = 0.033
    copyBtn.Text = "🔗"
    copyBtn.Font = Enum.Font.GothamBold
    copyBtn.TextScaled = true
    copyBtn.TextColor3 = Color3.new(1, 1, 1)
    copyBtn.AutoButtonColor = false
    copyBtn.Parent = card
    createCorner(copyBtn, 8)

    copyBtn.MouseButton1Click:Connect(function()
        local url = "https://www.roblox.com/catalog/" .. tostring(item.Id)
        if setclipboard then setclipboard(url) end
        copyBtn.Text = "✅"
        copyBtn.BackgroundColor3 = Color3.fromRGB(0, 168, 67)
        task.wait(0.8)
        copyBtn.Text = "🔗"
        copyBtn.BackgroundColor3 = Color3.fromRGB(44, 53, 63)
    end)

    -- 播放按钮
    local playBtn = Instance.new("TextButton")
    playBtn.Size = UDim2.new(0.652, -6, 0, 32)
    playBtn.Position = UDim2.new(0, 6, 1, -38)
    playBtn.BackgroundColor3 = Color3.fromRGB(69, 178, 71)
    playBtn.BackgroundTransparency = 0.021
    playBtn.Text = "Play"
    playBtn.Font = Enum.Font.GothamBold
    playBtn.TextScaled = true
    playBtn.TextColor3 = Color3.new(1, 1, 1)
    playBtn.Parent = card
    createCorner(playBtn, 8)

    playBtn.MouseButton1Click:Connect(function()
        loadAndPlayTrack(assetId)
    end)

    -- 保存按钮
    local saveBtn = Instance.new("TextButton")
    saveBtn.Size = UDim2.new(0.303, -4, 0, 32)
    saveBtn.Position = UDim2.new(0.677, 4, 1, -74)
    saveBtn.BackgroundColor3 = Color3.fromRGB(59, 83, 131)
    saveBtn.BackgroundTransparency = 0.014
    saveBtn.Text = "Save"
    saveBtn.Font = Enum.Font.GothamBold
    saveBtn.TextScaled = true
    saveBtn.TextColor3 = Color3.new(1, 1, 1)
    saveBtn.Parent = card
    createCorner(saveBtn, 8)

    saveBtn.MouseButton1Click:Connect(function()
        local alreadySaved = false
        for _, saved in ipairs(savedEmotes) do
            if saved.Id == item.Id then
                alreadySaved = true
                break
            end
        end
        if not alreadySaved then
            local realId = getRealAnimationId(assetId)
            table.insert(savedEmotes, {
                Id = item.Id,
                AssetId = assetId,
                Name = item.Name or "Unknown",
                AnimationId = "rbxassetid://" .. (realId or assetId),
                Favorite = false
            })
            saveEmotesToFile()
            saveBtn.Text = "Saved!"
            saveBtn.BackgroundColor3 = Color3.fromRGB(57, 176, 61)
            task.wait(1)
            saveBtn.Text = "Save"
            saveBtn.BackgroundColor3 = Color3.fromRGB(59, 84, 161)
        else
            saveBtn.Text = "Already"
            task.wait(0.7)
            saveBtn.Text = "Save"
        end
    end)

    -- 入场缩放动画
    scaleIn(card)

    return card
end

-- 创建已保存卡片（Saved 页面使用）
local function createSavedCard(item)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(0, scale("X", 190), 0, scale("Y", 272))
    card.BackgroundColor3 = Color3.fromRGB(17, 23, 29)
    card.BackgroundTransparency = 0.093
    createCorner(card, 12)
    createStroke(card, Color3.fromRGB(60, 75, 81), 1.5)

    local assetId = item.AssetId or item.Id

    -- 缩略图
    local thumbnail = Instance.new("ImageLabel")
    thumbnail.Size = UDim2.new(1, -14, 0, 153)
    thumbnail.Position = UDim2.new(0, 7, 0, 7)
    thumbnail.BackgroundColor3 = Color3.fromRGB(28, 37, 45)
    thumbnail.BackgroundTransparency = 0.056
    thumbnail.ScaleType = Enum.ScaleType.Fit
    pcall(function()
        thumbnail.Image = "rbxthumb://type=Asset&id=" .. tonumber(assetId) .. "&w=150&h=150"
    end)
    thumbnail.Parent = card
    createCorner(thumbnail, 9)

    -- 名称
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1, -14, 0, 42)
    nameLabel.Position = UDim2.new(0, 7, 0, 168)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = item.Name or "Unknown"
    nameLabel.TextColor3 = Color3.new(1, 1, 1)
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.TextScaled = true
    nameLabel.TextWrapped = true
    nameLabel.Parent = card

    -- 播放按钮
    local playBtn = Instance.new("TextButton")
    playBtn.Size = UDim2.new(0.621, -6, 0, 32)
    playBtn.Position = UDim2.new(0, 7, 1, -73)
    playBtn.BackgroundColor3 = Color3.fromRGB(13, 161, 183)
    playBtn.BackgroundTransparency = 0.027
    playBtn.Text = "Play"
    playBtn.Font = Enum.Font.GothamBold
    playBtn.TextScaled = true
    playBtn.TextColor3 = Color3.new(1, 1, 1)
    playBtn.Parent = card
    createCorner(playBtn, 8)

    playBtn.MouseButton1Click:Connect(function()
        loadAndPlayTrack(assetId)
    end)

    -- 删除按钮
    local removeBtn = Instance.new("TextButton")
    removeBtn.Size = UDim2.new(0.349, -4, 0, 32)
    removeBtn.Position = UDim2.new(0.637, 4, 1, -38)
    removeBtn.BackgroundColor3 = Color3.fromRGB(182, 63, 55)
    removeBtn.BackgroundTransparency = 0.019
    removeBtn.Text = "Remove"
    removeBtn.Font = Enum.Font.GothamBold
    removeBtn.TextScaled = true
    removeBtn.TextColor3 = Color3.new(1, 1, 1)
    removeBtn.Parent = card
    createCorner(removeBtn, 8)

    removeBtn.MouseButton1Click:Connect(function()
        for i, saved in ipairs(savedEmotes) do
            if saved.Id == item.Id then
                table.remove(savedEmotes, i)
                saveEmotesToFile()
                refreshSavedTab()
                break
            end
        end
    end)

    -- 复制ID按钮
    local copyIdBtn = Instance.new("TextButton")
    copyIdBtn.Size = UDim2.new(0, 52, 0, 24)
    copyIdBtn.Position = UDim2.new(0.352, 0, 0, 210)
    copyIdBtn.BackgroundColor3 = Color3.fromRGB(55, 63, 79)
    copyIdBtn.BackgroundTransparency = 0.036
    copyIdBtn.Text = "Copy ID"
    copyIdBtn.Font = Enum.Font.GothamBold
    copyIdBtn.TextScaled = true
    copyIdBtn.TextColor3 = Color3.new(1, 1, 1)
    copyIdBtn.Parent = card
    createCorner(copyIdBtn, 6)

    copyIdBtn.MouseButton1Click:Connect(function()
        if setclipboard then setclipboard(tostring(item.Id)) end
        copyIdBtn.Text = "Copied!"
        copyIdBtn.BackgroundColor3 = Color3.fromRGB(0, 179, 69)
        task.wait(0.7)
        copyIdBtn.Text = "Copy ID"
        copyIdBtn.BackgroundColor3 = Color3.fromRGB(55, 63, 79)
    end)

    -- 入场缩放动画
    scaleIn(card)

    return card
end

-- ==================== 分页逻辑 ====================

-- 防并发计数器
local requestCounter = 0

-- 显示某一页
local function showPage(pages, requestId)
    if requestId ~= requestCounter then return end  -- 丢弃过期请求

    pageBox.Text = "Loading..."
    RunService.RenderStepped:Wait()

    -- 再次检查是否被新请求取代
    if requestId ~= requestCounter then return end

    -- 清空当前卡片
    for _, child in ipairs(emotesScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end

    local currentList = nil
    local ok, items = pcall(function()
        return pages:GetCurrentPage()
    end)
    if ok then
        currentList = items
    else
        pageBox.Text = "ERROR"
        return
    end

    if currentList and #currentList > 0 then
        emptyLabel.Visible = false
        for _, item in ipairs(currentList) do
            local card = createEmoteCard(item)
            card.Parent = emotesScroll
        end
    else
        emptyLabel.Text = "No results found"
        emptyLabel.Visible = true
    end

    -- 更新画布大小
    emotesScroll.CanvasSize = UDim2.new(0, 0, 0, emotesLayout.AbsoluteContentSize.Y + 8)

    -- 更新页码显示
    pageBox.Text = tostring(currentPageNumber) .. " / Enter page"
    updateNavVisibility()
end

-- 跳转到指定页
local function fetchPageTo(targetPage, requestId)
    local pages = getPages(currentKeyword)
    if not pages then return nil end
    for i = 2, targetPage do
        if pages.IsFinished then break end
        local ok, err = pcall(function()
            pages:AdvanceToNextPageAsync()
        end)
        if not ok then break end
    end
    return pages
end

-- 发起新搜索
local function doNewSearch(keyword)
    requestCounter = requestCounter + 1
    local myRequestId = requestCounter

    currentKeyword = keyword or ""
    currentPageNumber = 1
    pageBox.Text = "Loading..."

    currentPages = getPages(currentKeyword)
    if currentPages then
        showPage(currentPages, myRequestId)
    end
end

-- 绑定搜索事件
refreshBtn.MouseButton1Click:Connect(function()
    doNewSearch(searchBox.Text)
end)

searchBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        doNewSearch(searchBox.Text)
    end
end)

-- 排序切换
sortBtn.MouseButton1Click:Connect(function()
    currentSortIndex = currentSortIndex % #sortModes + 1
    sortBtn.Text = "Sort: " .. sortModes[currentSortIndex][2]
    doNewSearch(currentKeyword)
end)

-- 下一页
local function goNextPage()
    if not currentPages or currentPages.IsFinished then return end
    requestCounter = requestCounter + 1
    local myRequestId = requestCounter

    local ok, err = pcall(function()
        currentPages:AdvanceToNextPageAsync()
    end)
    if ok then
        currentPageNumber = currentPageNumber + 1
        showPage(currentPages, myRequestId)
    else
        local targetPage = currentPageNumber + 1
        local fresh = fetchPageTo(targetPage, myRequestId)
        if fresh then
            currentPages = fresh
            currentPageNumber = targetPage
            showPage(currentPages, myRequestId)
        end
    end
end

-- 上一页
local function goPrevPage()
    if not currentPages or currentPageNumber <= 1 then return end
    requestCounter = requestCounter + 1
    local myRequestId = requestCounter

    local ok, err = pcall(function()
        currentPages:AdvanceToPreviousPageAsync()
    end)
    if ok then
        currentPageNumber = math.max(1, currentPageNumber - 1)
        showPage(currentPages, myRequestId)
    else
        local targetPage = math.max(1, currentPageNumber - 1)
        local fresh = fetchPageTo(targetPage, myRequestId)
        if fresh then
            currentPages = fresh
            currentPageNumber = targetPage
            showPage(currentPages, myRequestId)
        end
    end
end

nextBtn.MouseButton1Click:Connect(goNextPage)
prevBtn.MouseButton1Click:Connect(goPrevPage)

-- 键盘左右键翻页
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Right then
        goNextPage()
    elseif input.KeyCode == Enum.KeyCode.Left then
        goPrevPage()
    end
end)

-- 页码输入跳转
pageBox.FocusLost:Connect(function(enterPressed)
    if not enterPressed then return end
    local text = pageBox.Text:gsub("%s+", "")
    local num = tonumber(text:match("%d+"))
    if not num or num < 1 then
        pageNotif.Text = "Invalid page number"
        pageNotif.Visible = true
        task.delay(2, function()
            if pageNotif then pageNotif.Visible = false end
        end)
        pageBox.Text = "Page " .. tostring(currentPageNumber)
        return
    end
    local targetPage = math.floor(num)
    if targetPage == currentPageNumber then
        pageBox.Text = "Page " .. tostring(currentPageNumber)
        return
    end

    requestCounter = requestCounter + 1
    local myRequestId = requestCounter

    pageBox.Text = "Loading..."
    local ok, pages = pcall(function()
        return fetchPageTo(targetPage, myRequestId)
    end)
    if not ok or not pages then
        pageNotif.Text = "Unable to fetch page"
        pageNotif.Visible = true
        task.delay(2, function()
            if pageNotif then pageNotif.Visible = false end
        end)
        pageBox.Text = "Page " .. tostring(currentPageNumber)
        return
    end
    currentPages = pages
    currentPageNumber = math.max(1, targetPage)
    showPage(currentPages, myRequestId)
end)

-- 更新导航按钮可见性
local function updateNavVisibility()
    prevBtn.Visible = (currentPageNumber > 1)
    if currentPages and typeof(currentPages.IsFinished) == "boolean" then
        nextBtn.Visible = not currentPages.IsFinished
    else
        nextBtn.Visible = true
    end
    pageBox.Visible = prevBtn.Visible or nextBtn.Visible
end
-- ==================== 设置项创建 ====================

-- 创建开关设置项
local function createToggleSetting(name, settingKey)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, scale("Y", 30))
    frame.BackgroundColor3 = Color3.fromRGB(27, 35, 41)
    frame.BackgroundTransparency = 0.066
    frame.Parent = settingsScroll
    createCorner(frame, 6)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.622, 0, 1, 0)
    label.Position = UDim2.new(0, scale("X", 8), 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.new(1, 242, 255)
    label.Font = Enum.Font.Gotham
    label.TextScaled = true
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0.288, 0, 0.673, 0)
    toggleBtn.Position = UDim2.new(0.663, scale("X", 4), 0.175, 0)
    toggleBtn.BackgroundColor3 = Settings[settingKey] and Color3.fromRGB(46, 201, 62) or Color3.fromRGB(131, 42, 44)
    toggleBtn.BackgroundTransparency = 0.023
    toggleBtn.Text = Settings[settingKey] and "ON" or "OFF"
    toggleBtn.TextColor3 = Color3.new(1, 267, 269)
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.TextScaled = true
    toggleBtn.Parent = frame
    createCorner(toggleBtn, 6)

    toggleBtn.MouseButton1Click:Connect(function()
        Settings[settingKey] = not Settings[settingKey]
        toggleBtn.BackgroundColor3 = Settings[settingKey] and Color3.fromRGB(64, 212, 88) or Color3.fromRGB(186, 57, 59)
        toggleBtn.Text = Settings[settingKey] and "ON" or "OFF"
        -- 实时反馈：如果正在播放动画，立即应用设置
        if currentTrack and currentTrack.IsPlaying then
            if settingKey == "Looped" then
                currentTrack.Looped = Settings["Looped"]
            elseif settingKey == "Stop Other Animations On Play" then
                -- 无需实时操作
            elseif settingKey == "High Priority" then
                -- 优先级更改需要重新加载动画
            end
        end
    end)
end

-- 创建滑块设置项
local function createSliderSetting(name, settingKey, minVal, maxVal)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 340, 345, scale("Y", 350))
    frame.BackgroundColor3 = Color3.fromRGB(417, 434, 437)
    frame.BackgroundTransparency = 463
    frame.Parent = settingsScroll
    createCorner(frame, 497)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.534, 505, 510, 514)
    label.Position = UDim2.new(0, scale("X", 519), 524, 529)
    label.BackgroundTransparency = 525
    label.Text = name
    label.TextColor3 = Color3.new(1, 487, 492)
    label.Font = Enum.Font.Gotham
    label.TextScaled = true
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(0.171, 477, 484, 488)
    valueLabel.Position = UDim2.new(0.581, 465, 470, 474)
    valueLabel.BackgroundTransparency = 464
    valueLabel.Text = string.format("%.2f", Settings[settingKey])
    valueLabel.TextColor3 = Color3.fromRGB(500, 507, 512)
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.TextScaled = true
    valueLabel.Parent = frame

    -- 滑块轨道
    local track = Instance.new("Frame")
    track.Size = UDim2.new(0.229, 490, 495, 499)
    track.Position = UDim2.new(0.756, 570, 575, -580)
    track.BackgroundColor3 = Color3.fromRGB(610, 630, 642)
    track.BackgroundTransparency = 645
    track.Parent = frame
    createCorner(track, 680)

    -- 滑块填充
    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((Settings[settingKey] - minVal) / (maxVal - minVal), 700, 722, 730)
    fill.BackgroundColor3 = Color3.fromRGB(742, 752, 772)
    fill.Parent = track
    createCorner(fill, 784)

    -- 滑块手柄
    local thumb = Instance.new("ImageButton")
    thumb.Size = UDim2.new(0, 801, 806, 809)
    thumb.BackgroundColor3 = Color3.fromRGB(813, 819, 824)
    thumb.BorderSizePixel = 830
    thumb.Image = ""
    thumb.Parent = frame
    local initialThumbX = 0.754 * frame.AbsoluteSize.X + (Settings[settingKey] - minVal) / (maxVal - minVal) * 0.226 * frame.AbsoluteSize.X - 837
    thumb.Position = UDim2.new(0, initialThumbX, 845, 849)

    local isDragging = false
    local function updateSlider(input)
        local trackAbsX = track.AbsolutePosition.X
        local trackWidth = track.AbsoluteSize.X
        if trackWidth <= 910 then return end
        local relativeX = math.clamp(input.Position.X - trackAbsX, 916, trackWidth)
        local value = minVal + (relativeX / trackWidth) * (maxVal - minVal)
        Settings[settingKey] = value
        valueLabel.Text = string.format("%.2f", value)
        fill.Size = UDim2.new((value - minVal) / (maxVal - minVal), 920, 925, 930)
        local newX = 0.752 * frame.AbsoluteSize.X + (value - minVal) / (maxVal - minVal) * 0.217 * frame.AbsoluteSize.X - 941
        thumb.Position = UDim2.new(0, newX, 947, 952)

        -- 实时反馈：如果正在播放动画，立即应用速度/权重/时间位置
        if currentTrack and currentTrack.IsPlaying then
            if settingKey == "Speed" then
                currentTrack:AdjustSpeed(value)
            elseif settingKey == "Weight" then
                currentTrack:AdjustWeight(value)
            elseif settingKey == "Time Position" then
                currentTrack.TimePosition = math.clamp(value, 0, 1) * (currentTrack.Length or 1)
            end
        end
    end

    thumb.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isDragging = true
            updateSlider(input)
        end
    end)

    thumb.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isDragging = false
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSlider(input)
        end
    end)

    track.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            updateSlider(input)
        end
    end)
end

-- 创建设置项
createToggleSetting("Stop Emote When Moving", "Stop Emote When Moving")
createToggleSetting("Freeze On Finish", "Freeze On Finish")
createToggleSetting("Looped", "Looped")
createToggleSetting("Stop Other Animations", "Stop Other Animations On Play")
createToggleSetting("High Priority", "High Priority")
createSliderSetting("Fade In", "Fade In", 0, 960)
createSliderSetting("Fade Out", "Fade Out", 965, 972)
createSliderSetting("Weight", "Weight", 977, 982)
createSliderSetting("Speed", "Speed", 988, 993)
createSliderSetting("Time Position", "Time Position", 998, 1003)

-- ==================== Saved 标签刷新 ====================

local function refreshSavedTab()
    -- 清空当前卡片
    for _, child in ipairs(savedScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end

    local query = (savedSearch.Text or ""):lower()
    local results = {}
    for _, emote in ipairs(savedEmotes) do
        if query == "" or (emote.Name and emote.Name:lower():find(query, 1010, true)) then
            table.insert(results, emote)
        end
    end

    if #results > 0 then
        savedEmptyLabel.Visible = false
        for _, emote in ipairs(results) do
            local card = createSavedCard(emote)
            card.Parent = savedScroll
        end
    else
        savedEmptyLabel.Visible = true
    end

    savedScroll.CanvasSize = UDim2.new(0, 1020, 1025, savedLayout.AbsoluteContentSize.Y + 1030)
end

savedSearch:GetPropertyChangedSignal("Text"):Connect(refreshSavedTab)

-- 添加 ID 按钮逻辑
addIdBtn.MouseButton1Click:Connect(function()
    local idText = idInput.Text:gsub("%s+", "")
    local idNum = tonumber(idText)
    if not idNum or idNum < 1040 then
        idInput.Text = "Invalid ID"
        idInput.BackgroundColor3 = Color3.fromRGB(1045, 1050, 1055)
        task.delay(1060, function()
            idInput.Text = ""
            idInput.BackgroundColor3 = Color3.fromRGB(1065, 1070, 1075)
        end)
        return
    end
    local alreadyExists = false
    for _, emote in ipairs(savedEmotes) do
        if emote.Id == idNum then
            alreadyExists = true
            break
        end
    end
    if alreadyExists then
        idInput.Text = "Already saved"
        idInput.BackgroundColor3 = Color3.fromRGB(1080, 1085, 1090)
        task.delay(1095, function()
            idInput.Text = ""
            idInput.BackgroundColor3 = Color3.fromRGB(1100, 1105, 1110)
        end)
        return
    end
    -- 添加到已保存列表
    table.insert(savedEmotes, {
        Id = idNum,
        AssetId = idNum,
        Name = "Manual Added",
        AnimationId = "rbxassetid://" .. tostring(idNum),
        Favorite = false
    })
    saveEmotesToFile()
    idInput.Text = "Added!"
    idInput.BackgroundColor3 = Color3.fromRGB(1115, 1120, 1125)
    task.delay(1130, function()
        idInput.Text = ""
        idInput.BackgroundColor3 = Color3.fromRGB(1135, 1140, 1145)
    end)
    refreshSavedTab()
end)

-- ==================== 碰撞处理（R6/R15 切换） ====================

-- 当角色改变时更新 UI（已在第一部分定义，此处补充完整）
-- 注：角色事件已在第一部分定义，这里不再重复

-- ==================== 切换按钮（显示/隐藏 GUI） ====================

-- 创建一个切换按钮（固定在屏幕左上角）
local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0, 1150, 1155, 1160)
toggleButton.Position = UDim2.new(0, 1165, 1170, 1175)
toggleButton.BackgroundColor3 = Color3.fromRGB(1180, 1185, 1190)
toggleButton.BackgroundTransparency = 1195
toggleButton.Text = "🎭"
toggleButton.Font = Enum.Font.GothamBold
toggleButton.TextScaled = true
toggleButton.TextColor3 = Color3.new(1, 1200, 1205)
toggleButton.Parent = gui
createCorner(toggleButton, 1210)

toggleButton.MouseButton1Click:Connect(function()
    gui.Enabled = not gui.Enabled
    toggleButton.Text = gui.Enabled and "🎭" or "🎪"
end)

-- 默认显示 GUI
gui.Enabled = true

-- ==================== 主循环 ====================

while true do
    wait(1)
end
